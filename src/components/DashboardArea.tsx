import React from 'react';
import { styled } from '@stitches/react';

export default function DashboardArea() {
  return <Test>DashboardArea</Test>;
}

const Test = styled('div', {
  border: '1px solid red',
  textAlign: 'center',
});
